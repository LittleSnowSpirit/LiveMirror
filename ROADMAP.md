# LiveMirror 项目发展规划

通过 15 个结构化问题确定了项目方向。本文档是长期需求指南，后续开发直接从这里取需求。

---

## 一、产品定位

- **核心用户**：个人主播为主，架构保持开放性，方便未来扩展到 MCN、品牌方、服务商
- **产品形态**：移动端优先，PWA 应用（可安装到手机桌面）
- **核心价值**：AI 驱动的直播复盘分析，帮助主播提升直播效果

## 二、商业模式

- **收费模式**：按量付费（按分析次数/时长收费）
- **服务形态**：卖服务（SaaS），不是卖 API
- **安全级别**：基础安全（用户认证 + 权限控制）

## 三、技术方向

### 后端
- **数据库**：PostgreSQL（主存储）+ Redis（缓存/队列）
- **转写引擎**：DashScope API 优先，引擎接口做抽象层，方便切换
- **部署**：Docker 容器化，支持一键部署

### 前端
- **移动端**：PWA（Progressive Web App），可安装到手机桌面
- **桌面端**：保持 Web 形态，响应式设计

### 工程化
- **开发节奏**：持续发布（每天可发布）
- **技术债务**：工程化建设（CI/CD、自动化测试、代码质量检查）
- **数据源**：渐进式扩展（先本地上传 → 链接解析 → 第三方接入）

## 四、功能路线图

### P0 - 打磨主流程（当前优先）

1. **数据持久化**
   - PostgreSQL 集成，替换内存/文件存储
   - 分析结果、任务状态、用户数据入库
   - Redis 用于任务队列和结果缓存

2. **主流程优化**
   - 上传 → 转写 → AI 分析 → 生成报告，每一步做到极致
   - 转写引擎抽象层（DashScope API，可切换）
   - AI 分析深度优化（主动发现问题、具体建议、完整报告）

3. **PWA 适配**
   - 响应式布局，移动端优先
   - Service Worker 离线缓存
   - 可安装到桌面（manifest.json）

### P1 - 用户体系与商业化

4. **用户体系完善**
   - 用户主页、历史记录、个人设置
   - 按量计费系统（用量统计、账单、充值）

5. **报告与分享**
   - PDF/图片报告导出
   - 分享链接生成
   - 报告模板自定义

### P2 - 数据源扩展

6. **链接解析**
   - 输入直播回放链接，自动下载分析
   - 支持主流平台（抖音、快手、淘宝）

7. **第三方数据接入**
   - 对接平台 API 获取直播数据
   - 数据同步和增量更新

### P3 - 高级功能

8. **多直播间管理**
   - 用户可注册多个直播间
   - 持续跟踪分析，趋势变化

9. **实时分析**
   - 直播过程中实时分析
   - 弹幕情绪、话术提醒

## 五、技术架构约束

- **扩展性**：所有外部服务（ASR、AI 分析）做抽象接口，方便切换实现
- **Python 3.10+**：使用 `datetime.timezone.utc` 而非 `datetime.UTC`
- **Element Plus**：显式 import，不依赖 AutoImport（tree-shaking 友好）
- **测试**：后端 pytest，前端 vitest，测试文件与源文件同目录

## 六、验证标准

每个功能完成后需要通过：
1. 后端测试：`python3 -m pytest tests/ -v`
2. 前端测试：`npm run test`
3. 类型检查：`npm run typecheck`
4. 构建验证：`npm run build`
5. 人工验证：在浏览器/手机上实际使用功能
